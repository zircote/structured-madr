name: Validate ADRs

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'docs/decisions/**'
      - 'examples/**'
      - 'src/**'
      - 'schemas/**'
      - 'action.yml'
  pull_request:
    paths:
      - 'docs/decisions/**'
      - 'examples/**'
      - 'src/**'
      - 'schemas/**'
      - 'action.yml'

jobs:
  validate-project-adrs:
    name: Validate Project ADRs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Validate project ADRs
        # zircote/structured-madr@v1.1.0
        uses: zircote/structured-madr@1885ace5d825e2b2982505e28468dd4f5ba1d1c0
        with:
          path: docs/decisions
          strict: 'false'

  validate-examples:
    name: Validate Example ADRs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Validate example ADRs
        # zircote/structured-madr@v1.1.0
        uses: zircote/structured-madr@1885ace5d825e2b2982505e28468dd4f5ba1d1c0
        with:
          path: examples
          strict: 'false'
