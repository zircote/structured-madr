name: Validate ADRs

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'docs/decisions/**'
      - 'examples/**'
      - 'src/**'
      - 'schemas/**'
      - 'action.yml'
  pull_request:
    paths:
      - 'docs/decisions/**'
      - 'examples/**'
      - 'src/**'
      - 'schemas/**'
      - 'action.yml'

jobs:
  validate-project-adrs:
    name: Validate Project ADRs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Validate project ADRs
        # zircote/structured-madr@v1.1.0
        uses: zircote/structured-madr@f8331ed88f0334a07ac939efed45dbd00d926a7e
        with:
          path: docs/decisions
          strict: 'false'

  validate-examples:
    name: Validate Example ADRs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Validate example ADRs
        # zircote/structured-madr@v1.1.0
        uses: zircote/structured-madr@f8331ed88f0334a07ac939efed45dbd00d926a7e
        with:
          path: examples
          strict: 'false'
