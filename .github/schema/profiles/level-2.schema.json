{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://smadr.dev/schemas/mif-profile-level-2.schema.json",
  "title": "MIF conformance profile — Level 2 (Standard)",
  "description": "Level 2 = Level 1 + always-derivable standard fields (namespace, modified, temporal). Optional collections (relationships, entities) are validated by the base schema only when present (content-dependent).",
  "allOf": [
    { "$ref": "https://mif-spec.dev/schema/mif.schema.json" },
    {
      "type": "object",
      "required": ["namespace", "modified", "temporal"]
    }
  ]
}
